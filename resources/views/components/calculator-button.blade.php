<div class="col-6 d-flex justify-content-{{ $position }}">
    <button type="submit" 
            class="btn btn-{{ $variant }} btn-lg w-75 py-3"
            name="option" 
            value="{{ $value }}">
        {{ $text }}
    </button>
</div>